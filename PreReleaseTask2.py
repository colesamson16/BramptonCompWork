import random
import string

array = []
Member = 0

def newMember():

    name = input('Enter name: ')
    member = random.choice(string.hexdigits)

    dict.update({member: name})

def printMember():
